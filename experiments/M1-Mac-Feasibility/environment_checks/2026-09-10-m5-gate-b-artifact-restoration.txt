Track A — Gate B (NEW MACHINE): locked model artifact restoration + byte-level verification
Date (UTC): 2026-09-10T07:18:33Z (download start) – 2026-09-10T07:20:00Z (verified)
Branch: research/m5-gate-a-provenance
Research repo HEAD at time of this step: 6c288fa10588a92600e5eb9383c87a01abf97975

Authorization: Autonomous overnight session, "NEW-MAC GATE B ARTIFACT RESTORATION"
explicitly authorized. This is NOT model selection — the generator was already locked
to Qwen/Qwen3-1.7B (DECISION_LOG D-034). This step only restores the SAME, already-
locked artifact on the new Apple M5 machine and re-verifies it byte-for-byte.
Gate C (inference) was NOT authorized at the time of download; a single synthetic
infrastructure smoke was authorized separately later in the same session (see the
Gate C environment-check file, if present).

Purpose: INFRASTRUCTURE PROVENANCE. No inference occurred during this step. No dataset
was downloaded. No scientific metric was computed.

RELATIONSHIP TO THE PRIOR INTEL GATE B:
This does NOT replace or rewrite `2026-09-06-gate-b-model-download.txt`. That file
remains the historical record of the original Gate-B selection + download on the Intel
MacBook Pro. This file records only the re-download and re-verification of the identical
artifact on the M5. The selection rationale, neutral criteria, and provenance in the
2026-09-06 file are unchanged and are not repeated here.

================================================================================
1. Locked artifact identity (unchanged — from DECISION_LOG D-034 / D-035)
================================================================================

Generator model:        Qwen/Qwen3-1.7B
GGUF repository:         Qwen/Qwen3-1.7B-GGUF   (official first-party, author "Qwen")
Repo revision (pinned):  90862c4b9d2787eaed51d12237eafdfe7c5f6077
GGUF filename:           Qwen3-1.7B-Q8_0.gguf
Quantization:            Q8_0
Expected size:           1,834,426,016 bytes
Expected SHA-256:        061b54daade076b5d3362dac252678d17da8c68f07560be70818cace6590cb1a

================================================================================
2. Download (reproducible method — same as the Intel Gate B)
================================================================================

  $ mkdir -p ~/models/clsm/Qwen3-1.7B
  $ cd ~/models/clsm/Qwen3-1.7B
  $ curl -L --fail --retry 3 --retry-delay 5 -o Qwen3-1.7B-Q8_0.gguf \
      "https://huggingface.co/Qwen/Qwen3-1.7B-GGUF/resolve/90862c4b9d2787eaed51d12237eafdfe7c5f6077/Qwen3-1.7B-Q8_0.gguf"

  (the URL pins the exact commit sha, not a floating `main` reference)

  curl exit: 0
  transfer: 1749 MiB, ~50 s, no errors, HTTP 200

Local storage (OUTSIDE the git repository):
  ~/models/clsm/Qwen3-1.7B/Qwen3-1.7B-Q8_0.gguf

================================================================================
3. Byte-level verification (HARD-STOP gate)
================================================================================

  $ stat -f%z Qwen3-1.7B-Q8_0.gguf
  1834426016
  -> EXACT MATCH to the locked size (1,834,426,016 bytes).

  $ shasum -a 256 Qwen3-1.7B-Q8_0.gguf
  061b54daade076b5d3362dac252678d17da8c68f07560be70818cace6590cb1a  Qwen3-1.7B-Q8_0.gguf
  -> EXACT MATCH to the locked SHA-256 (full 64-hex-char value, not a prefix/suffix).

Both independent checks pass. No HARD STOP. The artifact on the M5 is bit-for-bit
identical to the artifact verified on the Intel machine on 2026-09-06.

================================================================================
4. Metadata-only inspection (pinned llama.cpp v0.4.0 tooling — NO inference)
================================================================================

Tool: `~/tools/llama.cpp/build/bin/llama-gguf <file> r`
  (the GGUF container reader shipped with the pinned llama.cpp build; it reads the
   header only — no prompt submitted, no token generated, no weights loaded for compute)

  gguf version:  3
  alignment:     32
  data offset:   5951136
  n_kv:          28
  n_tensors:     310
  general.architecture = qwen3

Full metadata (read via the local `gguf` Python package from
`~/tools/llama.cpp/gguf-py`, PYTHONPATH-only, run under the project .venv — no package
installed into the venv; read-only header parse, no inference):

  general.architecture        = qwen3
  general.name                = "Qwen3 1.7B Instruct"
  general.type                = model
  general.finetune            = Instruct
  general.basename            = Qwen3
  general.size_label          = 1.7B
  general.quantization_version = 2
  general.file_type           = 7   (Q8_0)
  qwen3.block_count           = 28
  qwen3.context_length        = 40960
  qwen3.embedding_length      = 2048
  qwen3.feed_forward_length   = (present)
  qwen3.attention.head_count      = 16
  qwen3.attention.head_count_kv   = 8
  qwen3.attention.key_length / value_length = (present)
  qwen3.rope.freq_base        = 1000000.0
  qwen3.attention.layer_norm_rms_epsilon = (present)
  tokenizer.ggml.model        = gpt2
  tokenizer.ggml.pre          = qwen2
  tokenizer.ggml.bos_token_id = 151643
  tokenizer.ggml.eos_token_id = 151645
  tokenizer.ggml.padding_token_id = 151643
  tokenizer.ggml.add_bos_token = false
  tokenizer.chat_template     = present, 4100 chars (ChatML `<|im_start|>`/`<|im_end|>`,
                                 explicit `<think>` / `</think>` reasoning markers)

CONTEXT-LENGTH DISCREPANCY (recorded, not resolved — same as the Intel Gate B noted):
  The Qwen3-1.7B *base model card* states 32,768 tokens; this GGUF's own embedded
  metadata reports `qwen3.context_length = 40960`. Both numbers are reported as found.
  Neither is asserted here as "the" context length; whichever is used at run time will
  be pinned in the run manifest.

None of the above is interpreted as scientific model performance. It is container/
tokenizer/template metadata only.

================================================================================
5. Integrity statement for this Gate-B restoration step
================================================================================

  - No model selection occurred. The generator remains Qwen/Qwen3-1.7B (D-034).
  - No alternative quantization was downloaded. No other candidate model was
    downloaded. No dataset was downloaded.
  - Exactly ONE model-weight GGUF was fetched, from the official first-party repo, at
    the exact pinned revision, and it matched the locked size and SHA-256 exactly.
  - No prompt was submitted; no token was generated; no inference of any kind ran
    during this step.
  - The artifact is stored outside the git repository. `.gitignore` already contains
    `*.gguf` (line 240) as defense-in-depth — re-confirmed still present.
  - Track B (configs/milestone1/, experiments/M1-English-Baseline/,
    experiments/MILESTONE_1_READINESS.md) is byte-unchanged.
